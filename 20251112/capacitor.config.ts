import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.lovable.6ae5d651183447af9298b3f40ea770ac',
  appName: 'nvidia-korea-pulse',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
